•.NET Framework: os를 가리지않음 

구조

클래스라이브러리> window Form asp.net ,aod.net, 기본클래스 라이브러리(BCL)

BCL(Basic Class Library): 기본 클래스

Window Form: 윈도우 응용 프로그램 제작을 위한 클래스 라이브러리

ASP.NET:웹 클래스 라이브러리

AOD.NET:데이터 클래스 라이브러리

CLR:컴파일된 C#코드를 실행하는 역할(vs를 통해 실행할수있는 *.exe 파일을 clr이 실행을 시켜주는 역할 ) 

MSLI,IL,중간언어: VS에서 C#를 컴파일한 코드 

CLS: 공통언어사용규칙 

C#: 포인터를 사용하지 않는다 

솔루션: 다수의 프로젝트를 하나의 솔루션 안에서 다룰 수 있음 

 

---비주얼 스튜디오

ctrl+shift+b:빌드

ctrl+f5 :콘솔확인 

f5:디버깅 

Console.Write(): 개행x =System.out.print()

Console.WirteLine(): 개행d=System.out.println()

namespace=패키지

staic메소드: 메인의 static은 객체를 생성하지 않고도 메인의 메소드를 사용할 수 있음

main은 프로그램의 진입점이다. main에 대한 부분을 static으로 명시해서 프로그램을 clr에서 분석해서 다시 사용할 수있도록해줌 정적메소드일경우 객체를 생성하지 않고도 만들 수 있음 

 

 

--메모

[void메소드]: 값을 반환하지 않는다.

-return

    -메소드를 호출한 영역으로 값을 반환

    - 메소드의 종료 

※ 반환형이 void로 선언된 메소드 내에서도 값의 반환이 아닌 메소드의 종료 목적으로 사용 



[new 키워드]

클래스명 a ; //참조변수 선언 

a= new 클래스명 : 생성된 인스턴스의 주소값이 반환되고 생성된 인스턴스의 주소값이 저장 

==> 참조변수는 인스턴스를 참조한다 ( 가리킨다)



