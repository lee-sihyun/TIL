[스택]

•가장 먼저 저장된 데이터가 가장 마지막에 빠져 나오는 자료구조

-LIFO (Last In First Out) : 먼저 저장된 데이터가 마지막에 빠져 나간다.

- 넣기(push),꺼내기 (pop)
3
2
1
위에서 부터 꺼낸다 (층층이 쌓고 위에서 부터 꺼냄)

[큐]

• 먼저 저장된 데이터가 먼저 빠져나간다.

-FIFO(First In First Out) :먼저 저장된 데이터가 먼저 빠져 나간다 

-넣기 (offer),꺼내기 (poll)

(넣기) 3 2 1 (꺼냄)



[큐 구현]

-인스턴스가 없거나 저장공간이 부족할때 예외가 발생하는 메소드 
•boolean add (E e) : 넣기 

•E remove() :꺼내기로 인스턴스의 참조값을 ㅎ반환하면서 해당 인스턴스를 저장소에서 삭제 

•E element() : 확인하기로 참조값을 반환하지만 삭제는 안함 



-예외를 발생하지 않고 해당 상황을 알리기 위한 특정값 반환(null or false)

•boolean off (E e) : 넣기, 넣을 공간이 부족하면 false 반환

•E poll(): 꺼내기, 꺼낼 대상 없으면 null반환

•E peek(): 확인하기, 확인할 대상이 없으면 null 반환



[덱 구현]



•덱(Deque)

public interface Deque<E> extends Queue<E>

-양방향으로 넣고 빼는 것이 가능한 자료구조 ( 큐는 한쪽 방향으로만 넣고 빼기 가능)

-스택 처럼 사용가능(큐처럼도 사용 가능) 

-공간이 부족할때 예외를 발생시킬 수 있는 메소드 

•앞으로 넣고, 꺼내고, 확인하기

void addFirst(E e): 넣기

E remove First() :꺼내기

E getFirst() : 확인하기

•뒤로 넣고, 꺼내고, 확인하기

void addLast(E e): 넣기

E removeLast() :꺼내기

E getLast() : 확인하기

- 예외를 발생하지 않고 특정값을 반환하는 메소드

•앞으로 넣고, 꺼내고, 확인

boolean offerFirst(E e) 넣기,공간이 부족하면 false 반환﻿

E pollFirst() :꺼내기, 꺼낼 대상 없으면 null 반환

E peekFirst(): 확인하기, 확인할 대상 없으면 null반환

•뒤로 넣고, 꺼내고 확인하기

boolean offerLast(E e): 넣기, 공간이 부족하면 false반환

E pollLast() :꺼내기, 꺼낼 대상 없으면 null 반환

E peekLast() : 확인하기, 확인할 대상 없으면 null반환



[스택 구현]

-스택이 필요하면 Deque<E>을 구현한 컬렉션 클래스의 인스턴스를 대상으로 메소드 호출﻿
•offerFirst & pollFirst : 앞으로 넣고 앞에서 꺼내기 

•offerLast & pollLast: 뒤로 넣도 뒤에서 꺼내기



[LinkedList<E>]

-LinkedList 는 덱, 리스트. 큐 인터페이스르 모두 구현한다 .
-타입별 참조변수의 선택에 따라 성격이 결정된다.

-연산

•스택에 넣기 push

•스택에서 꺼내기 pop  



[배열기반 스택]

interface DIStack<E>

class DCStack<E> implements DIStack<E> 

