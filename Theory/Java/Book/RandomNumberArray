[RandomNumber] 

java.utill.Random(); 클래스에 있는 것으로 난수생성을하여 복권과같이 랜덤의 숫자를 만듬 



[Arrays]

•public static int [] copyOf(int [] original, int newLength) : original에 전달된 배열을 첫번쨰 요소부터 newLength의 길이 만큼 복사한다 -> 새로운 배열을 생성하는 메소드 

첫번째 인자로 전달된 배열을 두번째 인자로 전달된 길아만큼 복사하되 새로운 배열을 생성하여 복사한다. 이렇게 생성된 배열의 참조값을 반환 



•public static int[] copyOfRange(int [] original, int from , int to ): original에 전달된 배열을 인덱스 from 부터 to이전요소까지 복사 

•public static void arraycopyu (Object src, int scrPos,Object dest, int desPos, int length): 배열 src의 srcPos배열 dest의 desPos로 length만큼 복사 -> 배열을 새로 생성하지 않고 존재하는 배열에 복사하려할때 사용 



•public static boolean equals (int [] a, int []a2] : 매개변수 a와 a2로 전달된 배열의 내용을 비교하여 t or f 반환

-> 배열에 저장된 데이터의 수 , 순서, 내용이 같다면 true 

•public static void sort(int [] a) : 매개변수 a로 전달된 배열을 오름차순으로 정렬 

•interface Comparble : int compareTo(Object o)메소드 구현을 통해 인스턴스의 순서 판단 기준 결정

•icompareTo  ( int compareTo(Object o))구현 방법

-인자로 전달된 o 가 작다면 양의 정수반환

-인자로 전달된 o가 크다면 음의 정수 반환

-인자로 전달된 o와 같아면 0을 반환



--> 인스턴스 대상의 크고 작음은 오름차순 정렬이후의 인스턴스 위치 이미 



•public static int binarySearch(int [] a, int key):배열 a에서 key를 찾아서 있으면 key의 인덱스값, 없으면 0보다 작은 수 반환 

-binarySearch : 이진탐색 알고리즘으로 정렬된 상태의 데이터를 대상으로 탐색하는 알고리즘( 반드시 정렬후 사용)

-탐색의 대상이되는 인스턴스들의 클래스는 Comparable인터페이스를 구현한 상태여야한다