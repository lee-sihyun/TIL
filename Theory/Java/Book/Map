[Map<K,V>]

-Key는 실질적인 값이 아니라 Value를 찾는 지표 

-Key는 중복 불가

-Key만 다르다면 Value는 중복 가능 



[ TreeMap<K,V>]

-트리 자료구조 기반으로 정렬상태 유지 

[Hash<K,V>]

-해쉬알고리즘 기반으로 구현 

-for-each,반복자룰 얻어 순차적 접근이 가능

-public Set<K> keySet() : Set<E>를 구현하는 컬렉션 인스턴스를 생성, 모든 Key를 담아 반환



